= form_for @mind, :html => { :class => 'form-horizontal' } do |f|
  .control-group
    = f.label :name, :class => 'control-label'
    .controls
      = f.text_field :name, :class => 'text_field'
  .control-group
    = f.label :description, :class => 'control-label'
    .controls
      = f.text_field :description, :class => 'text_field'
  .control-group
    = f.label :input_arity, :class => 'control-label'
    .controls
      = f.number_field :input_arity, :class => 'number_field'
  .control-group
    = f.label :output_arity, :class => 'control-label'
    .controls
      = f.number_field :output_arity, :class => 'number_field'
  .form-actions
    = f.submit nil, :class => 'btn btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), minds_path, :class => 'btn'
